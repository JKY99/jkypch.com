# 이미지 작업 가이드

## 프로필 사진

**파일 경로:** `/assets/images/profile.jpg`

- 권장 크기: **500×500px** 이상 (정사각형 비율 권장)
- 허용 형식: `.jpg`, `.png`, `.webp`
- `_config.yml`의 `author.avatar` 값이 이 경로를 가리키고 있음
- 배경이 단색이거나 깔끔한 증명사진/프로필 사진 권장

> 이 폴더에 `profile.jpg` 파일을 추가하면 사이드바에 자동으로 표시됩니다.
